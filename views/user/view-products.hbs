<section>
    <div class="container mt-5">
        <div class="row">
            {{#each products}}
                <div class="col-12 col-md-6 col-lg-4 col-xxl-3">
                    <div class="card" style="width: 18rem;">
                        <img src="/product-images/{{this._id}}.webp" class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title">{{this.name}}</h5>
                            <p class="card-text">{{this.description}}</p>
                            <button type="button" class="btn btn-success btn-add-cart" data-id="{{this._id}}">Add to cart</button>
                        </div>
                    </div>
                </div>
            {{/each}}
        </div>
    </div>
</section>
